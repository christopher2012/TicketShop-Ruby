<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "ticket app", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <% if logged_in? %>
            <li> <a class="no-pointer">Stan konta: <%= current_user.balance %> zł</a></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Konto <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <%@user = current_user %>
                <li><%= link_to "Profil", current_user %></li>
                <li><%= link_to "Ustawienia", edit_user_path(@user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Wyloguj", logout_path, method: :delete %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to "Zaloguj", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>